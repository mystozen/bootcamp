function runactivity() {
  let x = Number(prompt("What is your first number of your age?"));
  let y = Number(
    prompt("What is the last 2 diget number of your mobile phone?")
  );
  z = x + y;
  console.log(x + y);
  alert(z);
}
